# dotfiles

## Additional install

```bash
# HTML Formatting
brew install prettierd

# Blade Formatting
npm install -g blade-formatter
npm install -g rustywind

# Searching files
brew install fzf
brew install ripgrep
brew install fd
```
